worker_processes 1;

events {
  worker_connections 1024;
}

http {
  server {
    listen $PORT;

    set_by_lua_block $redirect_base_url {
        return os.getenv("REDIRECT_BASE_URL") or "https://www.betterwithbecky.com";
    }

    location / {
        return 301 $redirect_base_url;
    }
  }
}
